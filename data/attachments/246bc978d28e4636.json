{
  "name": "test_extra_root_field",
  "description": "Лишний ключ 'version' в корне Schema",
  "version": "1.0.0",
  "parameters": {
    "type": "object",
    "properties": {
      "prompt": {
        "type": "string",
        "description": "Текст для генерации",
        "default": "",
        "enum": []
      }
    },
    "required": [
      "api_key"
    ]
  }
}