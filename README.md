# Tests Function API Calling OpenAI

## 📑 Оглавление
- [📖 Описание проекта](#tests-function-api-calling-openai)
- [📂 Структура проекта](#-структура-проекта)
- [🔑 Получение OpenRouter API Key](#-получение-openrouter-api-key)
- [➕ Как добавлять новые функции](#-как-добавлять-новые-функции)
- [🧪 Как добавлять тестовые suites](#-как-добавлять-тестовые-suites)
- [🚀 Как запускать тесты](#-как-запускать-тесты)
- [📊 Результаты тестов](#-результаты-тестов)
- [⚙️ Настройки проекта](#️-настройки-проекта)
- [🗄️ Кэширование](#-кэширование)
- [🗄️ Переключение кеша через-env](#-переключение-кеша-через-env)
- [🛠 Best Practices](#-best-practices)


Этот проект предназначен для тестирования вызова функций через OpenAI API с использованием схем (JSON‑описаний).  
Он позволяет:
- Реализовывать функции в отдельном модуле (`src/functions/`).
- Автоматически регистрировать их в реестре (`src/function_register.py`).
- Проверять работу функций через тест‑раннер (`tests/test_runner.py`).
- Описывать тестовые сценарии в JSON‑файлах (`tests/suites/`).
- Сохранять результаты выполнения в `test_results/`.

## 📂 Структура проекта

```commandline
📂 Project Root
├── src/                          # исходный код
│   ├── functions/                # отдельные файлы с реализованными функциями
│   │   ├── get_weather_report.py
│   │   ├── trello_list_action.py
│   │   ├── add_num.py
│   │   ├── sqrt_num.py
│   │   └── ...
│   ├── function_register.py      # реестр функций и механизм регистрации
│   ├── function_validators.py    # проверки схем (required, типы, уникальность)
│   ├── cache_manager.py          # управление кэшем
│   └── openrouter_client.py      # клиент для работы с OpenRouter API
│
├── tests/                        # тестовые сценарии и runner
│   ├── test_runner.py            # основной тест‑раннер
│   └── suites/                   # JSON‑файлы с тестовыми наборами (suites)
│       ├── weather.json
│       ├── trello_list_action.json
│       ├── double_operation.json
│       └── ...
│
├── test_results/                 # результаты выполнения тестов (отчёты и exec)
│   ├── get_weather_report/
│   ├── trello_list_action/
│   ├── double_operation/
│   └── ...
│
├── configurates/                 
│   └── config.py                 # конфигурация (API‑ключи, токены)
│
├── requirements.txt              # зависимости проекта
├── .env                          # ключи и токены (не хранить в git)
├── .venv/                        # виртуальное окружение

```

## 🔑 Получение OpenRouter API Key

Для работы проекта требуется ключ OpenRouter API — без него тест‑раннер не сможет отправлять запросы к модели.

### Где получить ключ
1. Перейдите на сайт [OpenRouter](https://openrouter.ai/).
2. Зарегистрируйтесь или войдите в свой аккаунт.
3. В личном кабинете откройте раздел **API Keys**.
4. Сгенерируйте новый ключ и скопируйте его.

### Как прописать ключ
В файле `.env` добавьте строку:
```dotenv
OPENROUTER_API_KEY=your_openrouter_key_here
```
### Использование
- Ключ автоматически подхватывается через configurates/config.py.
- Никогда не коммитьте .env в репозиторий — добавьте его в .gitignore.
- Если ключ недействителен или отсутствует, тесты не будут выполняться.

## ➕ Как добавлять новые функции

1. Создать новый файл в `src/functions/`, например `src/functions/get_weather_report.py`.
2. Внутри файла:
   - Импортировать `function` из `src/function_register`.
   - Описать JSON‑схему функции (name, description, parameters).
   - Реализовать саму функцию.
   - Зарегистрировать её с помощью декоратора `@function`.

### Пример

```python
from src.function_register import function

GET_WEATHER_REPORT_SCHEMA = {
    "name": "get_weather_report",
    "description": "Получает прогноз погоды для указанного города",
    "parameters": {
        "type": "object",
        "properties": {
            "city": {"type": "string", "description": "Название города"},
            "date": {"type": "string", "description": "Дата (опционально)"}
        },
        "required": ["city"]
    },
}

@function(GET_WEATHER_REPORT_SCHEMA)
def get_weather_report(arguments: dict):
    city = arguments.get("city")
    date = arguments.get("date") or "today"
    return {
        "city": city,
        "date": date,
        "temperature": "5°C"
    }
```
3. После добавления файл автоматически импортируется при запуске test_runner.py.

## 🧪 Как добавлять тестовые suites

1. Создать JSON‑файл в папке `tests/suites/`, например: tests/suites/weather.json

2. Внутри описать тесты в формате массива объектов:
```json
[
  {
    "query": "Какая погода завтра в Минске?",
    "expected_function": "get_weather_report",
    "expected_arguments": {"city": "Минск", "date": "2025-12-08"}
  },
  {
    "query": "Погода в Берёзе сегодня",
    "expected_function": "get_weather_report",
    "expected_arguments": {"city": "Берёза"}
  }
]
```
3. Каждый тест содержит:
   - query — текст запроса на естественном языке.
   - expected_function — имя функции, которую должна вызвать модель.
   - expected_arguments — аргументы, которые модель должна извлечь из запроса.
4. Для последовательных операций можно добавить поле next_function:
```json
{
  "query": "Возьми число 8, прибавь 1 и потом вычисли квадратный корень",
  "expected_function": "add_num",
  "expected_arguments": {"num": 8},
  "next_function": {
    "name": "sqrt_num",
    "arguments": {"num": 9}
  }
}
```
## 🚀 Как запускать тесты

Тест‑раннер позволяет проверять работу функций на основе заранее подготовленных JSON‑сценариев.

### Пример запуска одной функции
```commandline
python -m tests.test_runner --function get_weather_report --tests tests/suites/weather.json
```
```commandline
python -m tests.test_runner --function add_num sqrt_num --tests tests/suites/double_operation.json
```
### Аргументы
   - --function — список функций, которые должны быть протестированы.
   - --tests — путь к JSON‑файлу с тестами.
### Автоматическая регистрация
Все файлы из src/functions/ импортируются автоматически при запуске test_runner.py. Функции регистрируются в реестре через декоратор @function.

## 📊 Результаты тестов

После запуска тестов результаты сохраняются в папке `test_results/`.

### Формат сохранения
- **tests_<timestamp>.json** — отчёт о прохождении тестов:
  - список тестов,
  - сравнение `expected_function` и `expected_arguments` с фактическими результатами,
  - статус (успех/ошибка),
  - цепочка вызовов (если несколько функций).

### Пример структуры
```commandline
test_results/ 
├── get_weather_report/
│ └── tests_2025-12-08T23-20.json 
├── add_num/ 
│ └── tests_2025-12-08T23-21.json
```

## ⚙️ Настройки проекта

### Конфигурация
- Все ключи и токены (например, Trello API, OpenRouter API) задаются в файле `.env` или через переменные окружения.
- Для удобства можно использовать `configurates/config.py`, где ключи читаются из `.env` и пробрасываются в код.

### .env
Пример содержимого `.env` (не хранить в git):

```dotenv
OPENROUTER_API_KEY=your_openrouter_key_here 
TRELLO_API_KEY=your_trello_key_here 
TRELLO_TOKEN=your_trello_token_here
```

### requirements.txt
Содержит все зависимости проекта:

## 🗄️ Кэширование

Для ускорения работы и снижения количества повторных запросов к OpenRouter API используется модуль `src/cache_manager.py`.

### Как работает
- Каждый запрос к модели сериализуется в ключ (JSON‑строка).
- Ответ сохраняется в локальном кеше.
- При повторном запуске тестов с теми же параметрами результат берётся из кеша, а не отправляется заново в API.

### Особенности
- Ключи сериализуются с `ensure_ascii=False`, чтобы поддерживать кириллицу и другие символы.
- Кэш хранится в локальных файлах (например, `.cache/` или внутри проекта).
- При изменении функций или схем рекомендуется очищать кеш, иначе можно получить устаревший результат.

## 🗄️ Переключение кеша через `.env`

Для управления кешированием можно добавить специальный флаг в файл `.env`.

### Пример
```dotenv
USE_CACHE=true
```

### Как работает
- Если `USE_CACHE=true` — модуль `cache_manager` будет использовать сохранённые результаты и не отправлять повторные запросы к API.
- Если `USE_CACHE=false` — кеш полностью отключается, все запросы выполняются напрямую через OpenRouter API.
- Значение читается в `configurates/config.py` и пробрасывается в код.

### Использование
- Включайте кеш при разработке и отладке, чтобы ускорить тесты.
- Отключайте кеш при изменении функций или схем, чтобы получать свежие результаты.


### Когда очищать кеш
   - После изменения схемы функции (parameters, required).
   - После изменения логики функции.
   - При добавлении новых тестов.

***Если результаты тестов не совпадают с ожидаемыми, а причина может быть в старых данных.***
Очистка кеша - Просто удалить файлы кеша

## 🛠 Best Practices

### Для функций
- Каждая функция должна находиться в отдельном файле в `src/functions/`.
- Имя функции в схеме (`"name"`) должно совпадать с тем, что используется в тестах (`expected_function`).
- В схеме указывайте `description` для каждого аргумента — это помогает модели правильно интерпретировать параметры.
- Используйте декоратор `@function(schema)` для регистрации.
