# =================================================================
# АКТИВНАЯ КОНФИГУРАЦИЯ (Участвует в работе только этот блок)
# =================================================================
router:
  name: "OPENROUTER"               # Для поиска API-ключа в api_keys_storage
  base_url: "https://openrouter.ai"
  role: "Ты инженер-программист. Твоя задача — строго следовать JSON-схеме."
  timeout: 60
  tool_choice: "auto"
  
  models:
    name: "openai/gpt-4o-mini"
    semaphore: 25
    max_tokens: 1000
    temperature: 0.3
    # Сюда можно дописывать любые поля OpenAI (напр. top_p: 0.9)

queries:
  - query: "Напиши функцию на Python для парсинга JSON"
  - query: "Объясни принцип работы асинхронности"

# =================================================================
# ШАБЛОНЫ И ЗАПАСНЫЕ МОДЕЛИ (Раскомментируйте и подставьте выше)
# =================================================================

# Провайдер KIE_AI:
# aggregator:
#   name: "KIE_AI"
#   base_url: "https://api.kie.ai"
#   models:
#     name: "gpt-4o-mini"
#     semaphore: 20
#     max_tokens: 1000
#     temperature: 0.3

# Модель GPT-5.1 Instant:
# models:
#   name: "openai/gpt-5.1-instant"
#   semaphore: 15
#   max_tokens: 2000
#   temperature: 0.5
